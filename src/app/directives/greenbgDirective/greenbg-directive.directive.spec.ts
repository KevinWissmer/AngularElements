import { GreenbgDirectiveDirective } from './greenbg-directive.directive';

describe('GreenbgDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new GreenbgDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
