import { HoverGreenbgDirectiveDirective } from './hover-greenbg-directive.directive';

describe('HoverGreenbgDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverGreenbgDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
